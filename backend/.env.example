# Copy this file to .env and fill in your real key. Never commit .env itself.
GEMINI_API_KEY=your-gemini-api-key-here

# Optional — only needed once/if the team decides to use Supabase for storage
SUPABASE_URL=
SUPABASE_KEY=
